#!/usr/bin/env bash
# ClaudeCluster environment setup

# Load MCP environment variables
dotenv_if_exists .env.mcp

# Load main environment file if it exists  
dotenv_if_exists .env

# Export project root
export CLAUDECLUSTER_PROJECT_ROOT="$(pwd)"

# Set log level for development
export LOG_LEVEL="${LOG_LEVEL:-INFO}"