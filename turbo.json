{
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": ["**/.env*", "tsconfig.json", ".eslintrc.js", ".prettierrc"],
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", ".next/**", "!.next/cache/**"],
      "inputs": ["src/**/*.tsx", "src/**/*.ts", "tsconfig.json", "package.json"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    },
    "test": {
      "dependsOn": ["^build"],
      "outputs": ["coverage/**"],
      "inputs": ["src/**/*.tsx", "src/**/*.ts", "test/**/*.ts", "test/**/*.tsx", "tests/**/*.ts", "tests/**/*.js"]
    },
    "lint": {
      "outputs": [".eslintcache"],
      "inputs": ["src/**/*.tsx", "src/**/*.ts", ".eslintrc.js"]
    },
    "lint:fix": {
      "outputs": [".eslintcache"],
      "inputs": ["src/**/*.tsx", "src/**/*.ts", ".eslintrc.js"]
    },
    "format": {
      "outputs": [],
      "inputs": ["src/**/*.tsx", "src/**/*.ts", "src/**/*.json", ".prettierrc"]
    },
    "clean": {
      "cache": false
    },
    "types:check": {
      "dependsOn": ["^build"],
      "inputs": ["src/**/*.tsx", "src/**/*.ts", "tsconfig.json"]
    }
  }
}
